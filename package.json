{
  "name": "safe-spot",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:web": "bun run --cwd apps/web dev",
    "dev:mobile": "bun run --cwd apps/mobile start",
    "dev:convex": "bunx convex dev"
  },
  "devDependencies": {
    "convex": "^1.22.0"
  }
}
